## 设备上传消息链路分析
完整的消息链路为：

1. 设备 → 通过MQTT协议发送数据到MQTT Broker（EMQX）
2. MQTT Broker → 接收设备数据并转发给后端服务
3. 后端服务 → 处理数据并发送到RabbitMQ
4. RabbitMQ → 消息队列，用于异步处理数据
5. 数据存储 → 存储到MySQL和TDengine
6. 前端 → 从后端API获取数据并展示