# 智慧能源决策系统（医院版）Spec 规格说明

> 基于 deep-ems0（祝融 EMS 能源管理系统）演进的新一代产品规格。
> 本文档为 Spec 驱动开发的总纲，后续所有开发均以本文档 + checklist.md + tasks.md 为准。

***

## 1. 项目背景

deep-ems0 是一套基于 Ruoyi + Spring Boot 构建的企业级能源管理系统（EMS），已具备数据采集（MQTT）、设备管理、能耗分析、报警管理、定额管理、运维管理、碳资产管理、数据看板、充电桩等能力。

现有系统主要面向通用工业园区/企业场景。现需在其基础上**新增**一套**适用于（海外/国内）医院场景的智慧能源决策系统**。

> **重要前提（加性开发）**：原系统全部功能**完整保留、不做删改**，继续可用。医院智慧能源决策系统作为**新增能力**，在原工程（后端 zhurong-ems-*、前端 Vue）上以新增模块/页面方式叠加开发，不替换、不删除原有业务。

本产品新增方向的关键差异：

- **UI 变化**：在保留原有页面的前提下，新增医院风格界面与医院大屏，支持国际化（中/英）。
- **功能调整**：新增监测对象聚焦**医院检查检验设备**（CT、MRI、DR、超声、检验流水线、DSA 等）。
- **数据采集方式**：新增通过 **IOT 平台回调本平台**（HTTP 回调）方式实现数据采集，区别于现有 MQTT 直采（MQTT 直采能力保留）。

## 2. 项目目标

1. 交付一套可同时面向国内/海外医院交付的智慧能源决策系统。
2. 通过 IOT 平台回调方式稳定采集医院检查检验设备的运行与能耗数据。
3. 提供面向医院管理者的能耗监测、能效分析与能源决策支持能力。
4. 复用并增强 deep-ems0 现有能力，降低重复开发成本。

## 3. 范围界定

### 3.1 保留基础（现有 deep-ems0 能力，原样保留）

> 以下能力**全部保留、不做删改**，同时可被医院场景复用/扩展。

| 领域    | 复用内容                                                                                  |
| ----- | ------------------------------------------------------------------------------------- |
| 技术栈   | Java 8 / Spring Boot 2.7.9 / MyBatis Plus / Sa-Token / Vue 2.6 / Element UI / ECharts |
| 存储    | MySQL 8.0（业务）、TDengine（时序，可选）、Redis 7.2（缓存）                                           |
| 消息    | RabbitMQ 3.12（异步）、MQTT（现有直采，保留）                                                       |
| 基础功能  | 系统管理（用户/角色/菜单/部门/字典/参数）、操作日志、登录日志、系统监控                                                |
| 可复用模块 | 设备管理、能耗分析、报警管理、定额管理、运维管理、碳资产管理、数据看板、充电桩、报表等（全部保留） |

### 3.2 新增范围（医院智慧能源决策系统，叠加开发）

| 领域      | 新增内容                                         |
| ------- | -------------------------------------------- |
| 前端      | 新增医院风格 UI 主题与医院大屏（原页面保留），新增国际化（中/英）         |
| 数据采集    | 新增 IOT 平台回调接入（HTTP 回调 → 本平台），鉴权、签名校验、数据解析、标准化落库 |
| 业务功能    | 新增医院检查检验设备监测、分区能耗分析、设备能效评估、节能建议、决策支持           |
| 多租户/多院区 | 新增院区隔离、数据权限（海外医院需求）                            |

### 3.3 非目标（本期不做）

- 不重做通用 EMS 的运维/巡检/定额等通用能力（**原功能保留复用**，不重复开发）。

- 不实现 IOT 平台本身（假设 IOT 平台已存在，负责设备接入与数据推送）。

- 不删除、不替换原有任意功能模块。

## 4. 用户角色

| 角色           | 说明                  |
| ------------ | ------------------- |
| 决策层（院长/分管领导） | 查看全院能耗概览、决策大屏、分析报告  |
| 能源管理负责人      | 能耗分析、能效评估、节能建议、报告导出 |
| 设备科/后勤       | 检查检验设备台账、运行监测、报警处理  |
| IOT 运维人员     | 回调接入配置、设备绑定、数据质量监控  |
| 系统管理员        | 用户/角色/权限/参数/字典管理    |

## 5. 功能需求

> 优先级：P0 = 必须（首期交付）、P1 = 应该（二期）、P2 = 可选（后续）

### 5.1 IOT 数据接入（P0）

- **FR-1 回调接收接口**：提供 HTTP 回调端点，接收 IOT 平台推送的数据（设备运行状态、能耗、开关机等）。

- **FR-2 安全鉴权**：回调请求需校验（Token / 签名 / IP 白名单，配置化）。

- **FR-3 数据解析与标准化**：将 IOT 原始报文解析为标准数据结构（设备标识、指标、值、时间戳、质量戳）。

- **FR-4 数据落库**：业务配置与元数据入 MySQL，时序/高频数据入 TDengine（可开关）。

- **FR-5 回调状态与重试**：失败记录、重试队列（RabbitMQ）、回调日志与监控。

- **FR-6 设备绑定管理**：将 IOT 平台设备 ID 绑定到本系统设备台账。

### 5.2 医院检查检验设备监测（P0）

- **FR-7 设备台账**：管理检查检验设备（CT、MRI、DR、超声、检验流水线、DSA 等）档案信息。

- **FR-8 运行状态监测**：实时/准实时展示设备在线、运行、待机、故障状态。

- **FR-9 设备能耗监测**：实时功率、累计电量、待机能耗识别。

- **FR-10 设备异常报警**：过载、离线、能耗突增、运行时长超限等报警规则与通知。

### 5.3 能耗分析与决策支持（P1）

- **FR-11 全院能耗概览**：按院区/楼宇/科室/设备层级钻取。

- **FR-12 分区/分项分析**：门诊/住院/医技/后勤分区，照明/空调/医疗设备/动力分项，同比环比、趋势、排名。

- **FR-13 设备能效评估**：单台/同类设备的单位工作量能耗、待机占比、运行效率评估。

- **FR-14 节能建议**：识别待机浪费、高耗能时段、异常设备，生成节能建议清单（决策支持）。

- **FR-15 分析报告**：周期报告生成与导出（对接现有报表能力）。

### 5.4 数据看板与大屏（P1）

- **FR-16 医院大屏**：医院风格可视化大屏（全院能耗、设备状态、报警、关键指标）。

- **FR-17 实时看板**：按角色定制的数据看板。

- **FR-18 国际化**：界面中/英切换，海外部署适配（多语言、多时区、单位体系）。

### 5.5 系统管理（P0）

- **FR-19 用户/角色/权限/菜单/部门**：复用现有系统管理。

- **FR-20 多院区数据隔离**：按院区隔离数据与权限（海外多院区医院）。

## 6. 非功能需求

| 类别  | 要求                                            |
| --- | --------------------------------------------- |
| 性能  | 回调接口吞吐 ≥ 1000 TPS（峰值）；普通查询 P95 ≤ 1s；大屏首屏 ≤ 3s |
| 安全  | 回调鉴权、HTTPS、密码策略、敏感数据加密、审计日志                   |
| 可用性 | 核心链路可用性 ≥ 99.9%；回调失败可重试不丢数据                   |
| 可扩展 | 设备/院区横向扩展；新设备类型可配置接入                          |
| 国际化 | 中/英；海外时区、币种（可选）                               |
| 兼容  | 复用 deep-ems0 技术栈与部署方式（Docker）                 |

## 7. 架构设计

```
┌─────────────┐   IOT 平台(设备接入侧，外部)          ┌──────────────────────────────┐
│  医院设备    │     检查检验设备运行/能耗数据             │     本系统（deep-ems0 演进）      │
│ CT/MRI/DR…  │ ──────────┐                           │                                │
└─────────────┘           ▼                           │  ┌────────────────────────┐    │
┌─────────────┐  ┌──────────────┐   HTTP 回调         │  │ 回调接入层              │    │
│  IOT 平台    │──▶ 回调服务      │─────────────────────▶│  │ 鉴权/签名→解析→标准化→MQ │    │
│（外部系统）   │  └──────────────┘                     │  └───────────┬────────────┘    │
└─────────────┘                                        │              ▼ (RabbitMQ)      │
                                                       │  ┌────────────────────────┐    │
                                                       │  │ 数据消费/落库           │    │
                                                       │  │ MySQL(业务) + TDengine  │    │
                                                       │  └───────────┬────────────┘    │
                                                       │              ▼                │
                                                       │  ┌────────────────────────┐    │
                                                       │  │ 应用层(分析/报警/决策)   │    │
                                                       │  └───────────┬────────────┘    │
                                                       │              ▼                │
                                                       │  ┌────────────────────────┐    │
                                                       │  │ 前端(医院UI/大屏/国际化)  │    │
                                                       │  └────────────────────────┘    │
                                                       └──────────────────────────────┘
```

**关键数据流**：IOT 平台采集设备数据 → 回调本平台 → 鉴权校验 → 解析标准化 → 投递 MQ → 异步落库（MySQL + TDengine）→ 分析/报警/大屏消费。

> **加性说明**：以上为**新增**链路，与现有 MQTT 直采链路并存互不影响；原有前端页面、后端接口、数据库表**全部保留**，新增模块在既有工程结构（zhurong-ems-admin 等）中叠加。

## 8. 里程碑规划

| 里程碑 | 内容                       | 依赖    |
| --- | ------------------------ | ----- |
| M1  | 新增回调接入层、设备台账、基础监测（新增模块）     | 无     |
| M2  | 新增医院 UI 主题 + 国际化、设备监测页面、报警 | M1    |
| M3  | 新增能耗分析 + 能效评估 + 节能建议（决策支持） | M1/M2 |
| M4  | 新增大屏、看板、分析报告、海外适配收尾        | M2/M3 |

> 各里程碑均在**保留原系统全部功能**的前提下增量交付，每阶段上线不影响原业务。

## 9. 风险与假设

- **假设**：IOT 平台已存在且支持向本系统 HTTP 回调推送设备数据；回调报文格式/协议可协商。

- **风险**：海外医院数据合规（如 GDPR）——需数据最小化、可审计；多时区/多语言适配工作量。

- **风险**：检查检验设备协议多样——通过 IOT 平台统一屏蔽，本系统只消费标准化数据。

## 10. 术语表

| 术语     | 说明                              |
| ------ | ------------------------------- |
| EMS    | Energy Management System，能源管理系统 |
| IOT 平台 | 物联网设备接入与数据汇聚平台（外部系统）            |
| 回调     | IOT 平台主动推送数据到本系统的 HTTP 接口机制     |
| 检查检验设备 | 医院 CT/MRI/DR/超声/检验流水线/DSA 等医疗设备 |
| 院区     | 医院物理/逻辑分区，用于数据隔离                |

